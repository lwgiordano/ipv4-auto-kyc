# CLAUDE.md

See [AGENTS.md](AGENTS.md) for the full project guide.

In short: use `./manage.sh` for everything — `setup`, `doctor`, `fmt`, `lint`,
`test` — rather than invoking toolchains directly. If the project seems broken,
run `./manage.sh doctor` first; it tells you what's miswired.
